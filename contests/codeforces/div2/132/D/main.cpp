#include <bits/stdc++.h>
using namespace std;
int main(){
   ios_base::sync_with_stdio(false);
   cin.tie(0);
   int n, m,q;
   cin>>n>>m;
   vector<int> a(m, 0);
   for(auto &i:a)cin>>i;
   cin>>q;
   cout<<q<<endl;
   for(int i = 0 ; i < q; i++){
      int xs, ys, xf, yf, k;
      cin>>xs>>ys>>xf>>yf>>k;
   }
   return 0;
}
